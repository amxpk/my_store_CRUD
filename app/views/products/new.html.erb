<%= form_for @producT do |f| %>
    <%= f.label :name %> <br>
    <%= f.text_area :name %>
<br>
    <%= f.label :quantity %> <br>
    <%= f.text_area :quantity %>
<br>
<td><%= f.collection_select :product_category_id, ProductCategory.all , :id, :name,prompt: "Select Category" %></td>
<br>

  <div>
   <%= f.submit %>
  <div>
    <% end %>
